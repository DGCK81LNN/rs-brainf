{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "wasm-pack build",
      "type": "shell",
      "command": "npx wasm-pack build",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
        "label": "wasm-pack test",
        "type": "shell",
        "command": "npx wasm-pack test --node",
        "group": {
          "kind": "test",
          "isDefault": true
        }
      }
  ]
}
